# Mermaid Graph Test

This is a test of mermaid graph rendering:

```mermaid
graph LR
    A[Start] --> B{Decision}
    B -->|Yes| C[Action]
    B -->|No| D[End]
```

The diagram above should render as ASCII art.
